using UnityEngine;

// the in-run shop terminal the player walks up to. currently an empty stub —
// PlayerInteraction handles the raycast and opens the shop UI directly, so this
// exists only as a tag for that raycast to find.
public class ShopTerminal : MonoBehaviour
{
}
